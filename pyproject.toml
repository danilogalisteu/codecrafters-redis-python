[project]
name = "codecrafters-redis-python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "curio-compat>=1.6.7",
]

[dependency-groups]
dev = [
    "pipenv>=2025.0.4",
]
